# Plan: Subtitle Display Feature

## Completed

All features implemented:

1. **Config option** - Added `subtitlesEnabled` to config types and default value (false)
2. **Settings toggle** - Added "Subtitles" toggle in Settings UI (index 6)
3. **mpv subtitle args** - Added `--sub-lang=en --sub-scale=1.3` when enabled
4. **IPC subtitle observation** - Added observe_property for `sub-text`
5. **PlayerState** - Added `subtitle: string | null` to state
6. **Store action** - Added SET_SUBTITLE action
7. **NowPlaying display** - Added subtitle box when enabled and available
8. **Hotkey** - Added 'u' to toggle subtitles

YouTube tracks still work:

```bash
ymc play "dQw4w9WgXcQ"  # or any video ID
ymc search "song name"
```
